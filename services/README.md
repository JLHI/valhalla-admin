# Services

Application services and Docker build contexts.

- `django/` — Django web app, Celery worker and scheduler.
- Valhalla server container configured per-graph.

See `compose/README.md` for orchestration details.