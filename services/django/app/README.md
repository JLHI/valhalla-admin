# Django App

Project app entry point.

- `manage.py`: Django management.
- `valhalla_admin/`: main application modules.
- Static and templates under `valhalla_admin/static` and `valhalla_admin/templates`.

Run:
- Via Docker Compose; see top-level README.